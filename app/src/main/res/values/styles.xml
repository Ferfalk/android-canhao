<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@drawable/shape_background</item>
    </style>

    <style name="button" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/shape_button</item>
        <item name="android:paddingBottom">@dimen/button_padding_bottom</item>
        <item name="android:paddingLeft">@dimen/button_padding_sides</item>
        <item name="android:paddingRight">@dimen/button_padding_sides</item>
        <item name="android:width">@dimen/button_large_width</item>
        <item name="android:height">@dimen/button_large_height</item>
    </style>
</resources>
